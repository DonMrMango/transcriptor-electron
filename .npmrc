# Usar versiones exactas - no permitir actualizaciones automáticas
save-exact=true

# Forzar que npm use el package-lock.json
package-lock=true

# No permitir actualizaciones automáticas de paquetes
save-prefix=""

# Usar cache local
prefer-offline=true

# No ejecutar scripts de instalación automáticamente (seguridad)
ignore-scripts=false
